# Tests and type checking. Not installed by install.sh: nothing here should end
# up inside an app that has Accessibility permission.
-r requirements.txt
# Only scripts/make_sounds.py needs soundfile now; the app itself never touches
# audio files, so it stays out of the runtime venv.
soundfile==0.14.0
pytest==9.1.1
mypy==2.3.0
types-pynput==1.8.1.20260712
